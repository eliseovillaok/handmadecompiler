/# INICIO DEL PROGRAMA CORRECTO #/
program
begin

	UINTEGER FUN f2 (UINTEGER y)
	begin
		y := y+4;
		RET (y);
	end

	single FUN f1 (UINTEGER x)
	begin
		x := f2(1);
		RET (TOS(x));
	end


	single z;
	z := f1(0);
	OUTF(z);

end